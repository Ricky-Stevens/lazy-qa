node_modules/
runs/
.env
.env.*.local
*.log
.DS_Store
dist/
coverage/
.vitest/
.idea/
.claude/
.context-guardian/
.plans/
.serena/
docs/
CLAUDE.md

# Local YAML configs — keep them off the public repo. The only config that
# ships in git is the reference example. Everything else (work portals,
# personal targets, throwaway scratchpads) stays local.
config/*
!config/example.yaml
